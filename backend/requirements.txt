openai>=2.0.0
pydantic-settings>=2.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
loguru>=0.7.0

# RAG
numpy>=1.26
sentence-transformers>=5.0.0
chromadb>=1.0.0
# torch 是 sentence-transformers 的依赖；CPU 版请单独装（否则默认拉 2GB+ 的 CUDA 版）：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# 记忆模块：Redis(短期) + MySQL(长期)
redis>=5.0
aiomysql>=0.2
cryptography>=42.0   # MySQL 8 的 caching_sha2_password 认证需要
